generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Comidas {
  id           Int @id @default (autoincrement())
  nome         String
  tipo         String
  preco        Float
  descricao    String

@@map ("comidas")
}